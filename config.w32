// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("yext", "for yext support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("yext", "enable yext support", "no");

if (PHP_YAF_EXT != "no") {
	EXTENSION("yext", "yext.c");
}

